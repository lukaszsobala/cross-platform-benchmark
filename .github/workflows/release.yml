# Build cpcpub for every target and attach the binaries to a release.
#
# These binaries are what "verified" means on a results hub: the hub loads the
# verified-builds.json produced here and marks a run whose build.binary_sha256
# matches one of these digests. So this workflow is not a convenience -- it is
# the definition of a comparable result, and everything it publishes has to be
# checkable from the outside: the exact compiler, the exact flags, and a digest
# of the exact file that was uploaded.
#
# Why the runner images are pinned to 24.04 and not to the newest thing going:
# the build host sets the *glibc floor* of what it produces. A binary linked
# against 26.04's glibc will not start on 24.04 or Debian 13 ("version
# `GLIBC_2.4x' not found"), while one built here runs on both, 26.04 included.
# Release artifacts want the oldest image still supported, and the jobs below
# print the highest glibc symbol version they ended up needing so that floor is
# a published fact rather than a hope.
#
# The macOS row is the exception to all of this and is worth knowing about: it
# is the only artifact that is neither cross-built nor emulated, because there
# is no Mach-O cross-toolchain here and nothing to run the result under. It is
# checked as far as a native runner can check it -- single-architecture arm64,
# ad-hoc signed, self-digest verified, smoke-tested -- and no further.
#
# The Windows rows build natively too, and for a plainer reason: the runners
# exist, so there is no argument for cross-building and then hoping. PE has no
# glibc floor to print; what stands in its place is the import table, which the
# check below reads and asserts against, because Windows has two ways to ship a
# binary that starts on the build machine and fails on the user's. One is a
# mingw runtime DLL the artifact expects to find beside it (hence -static in
# bench/Makefile). The other is quieter: a mingw-w64 built against msvcrt rather
# than the UCRT substitutes a pre-C99 printf, and "%zu" then prints as the
# letters z and u inside a document meant to be machine-read.
#
# The Windows-on-Arm row carries its own toolchain. GCC has no aarch64 Windows
# target, and the `gcc` on that image is an x86_64 one running under emulation
# -- it builds, it runs, and it produces the wrong architecture -- so that row
# fetches llvm-mingw at a pinned version and a pinned digest instead.
#
# This is separate from what the binaries *target*. RVA23 comes from -march, not
# from the host distribution -- but it does need a compiler new enough to know
# the extension names, and the newest riscv64 cross-gcc 24.04 has is 14, which
# rejects Zimop and Zcmop outright ("unsupported standard extension"). GCC 15 is
# the floor for the profile, so that one row builds on 26.04 and states the floor
# it needs; the RISC-V jobs install the newest cross-gcc their image offers, and
# the build is checked against the ELF arch attributes afterwards, so a toolchain
# that quietly produced less than RVA23 fails the job instead of shipping.
#
# The cost of that row's newer image is a higher glibc floor for that one binary
# -- which is the right trade: RVA23 silicon is newer than 24.04 anyway, and the
# job prints the floor it ended up with. The portable rv64gc row must stay on
# 24.04 for the opposite reason, spelled out at the arch check below.
#
# Secrets: none are used and none exist to leak. The only credential in play is
# the automatic, per-run GITHUB_TOKEN, given to `gh` through the environment --
# never on a command line, never echoed, never written to a file. Permissions
# are empty by default and read-only on the build jobs; exactly one job may
# write, and only to attach assets. The build jobs check out with
# persist-credentials disabled, so the token is not left in .git/config while a
# Makefile and a compiler run.

name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "existing tag to build and upload to"
        required: true

permissions: {}

concurrency:
  group: release-${{ github.event.inputs.tag || github.ref_name }}
  cancel-in-progress: false

env:
  # One source of truth for what is being built, so a run cannot publish to a
  # tag it did not check out.
  TAG: ${{ github.event.inputs.tag || github.ref_name }}

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runs-on }}
    permissions:
      contents: read
    defaults:
      run:
        # Windows runners default to PowerShell. One shell for every row keeps
        # the steps below readable as the single script they are.
        shell: bash
    env:
      # `make` produces cpcpub.exe on Windows and cpcpub everywhere else, so the
      # steps name the binary through here rather than repeating that.
      BIN: bench/cpcpub${{ matrix.exe }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # x86-64 baseline: runs on anything 64-bit Intel or AMD.
          - name: x86_64
            runs-on: ubuntu-24.04
            asset: cpcpub-linux-x86_64
            vars: MARCH=x86-64 MTUNE=generic
            target: x86_64
          # x86-64-v3 (AVX2 + FMA): the x86 build where all four variants
          # differ, so `--variants` has something to say on this ISA.
          - name: x86_64-v3
            runs-on: ubuntu-24.04
            asset: cpcpub-linux-x86_64-v3
            vars: MARCH=x86-64-v3 MTUNE=generic
            target: x86_64
          # Native arm64 runner, free for public repositories. If this repo
          # ever goes private, cross-build it with gcc-aarch64-linux-gnu and a
          # qemu-aarch64 smoke test, exactly as the riscv64 rows do.
          - name: aarch64
            runs-on: ubuntu-24.04-arm
            asset: cpcpub-linux-aarch64
            vars: MARCH=armv8-a MTUNE=generic
            target: aarch64
          # rv64gc: the RISC-V build that runs everywhere, including the boards
          # `make sg2000` exists for.
          - name: riscv64
            runs-on: ubuntu-24.04
            asset: cpcpub-linux-riscv64
            cross: riscv64-linux-gnu
            vars: MARCH=rv64gc
            target: riscv64
            qemu-cpu: rv64
          # RVA23U64: the profile Ubuntu 26.04 takes as its riscv64 baseline.
          # `make rva23` picks whichever -march spelling this toolchain knows,
          # and the flags it settled on are recorded in the manifest below.
          # On 26.04 rather than 24.04 for two things 24.04 cannot supply: a
          # cross-gcc that knows Zimop/Zcmop (15+, min-gcc below), and a
          # qemu-user that knows `-cpu rva23u64` for the smoke test.
          - name: riscv64-rva23
            runs-on: ubuntu-26.04
            asset: cpcpub-linux-riscv64-rva23
            cross: riscv64-linux-gnu
            make-target: rva23
            min-gcc: 15
            target: riscv64
            qemu-cpu: rva23u64
          # LoongArch: the base loongarch64 ISA (LA64 with the lp64d ABI), not
          # la464 or la664, so the artifact is not tied to one vendor's core.
          # Cross-built and smoke-tested under qemu exactly as rv64gc is; 24.04
          # carries gcc-14 for this target, which is well past the point where
          # LoongArch support settled.
          - name: loongarch64
            runs-on: ubuntu-24.04
            asset: cpcpub-linux-loongarch64
            cross: loongarch64-linux-gnu
            vars: MARCH=loongarch64
            target: loongarch64
            qemu-cpu: la464
          # Apple Silicon. A native arm64 macOS runner, because there is no
          # cross-toolchain for a Mach-O target here and nothing to emulate it
          # with -- so unlike every row above, what this one publishes is only
          # as checked as the runner that built it.
          #
          # MACOSX_DEPLOYMENT_TARGET is the glibc-floor argument in Mach-O form:
          # clang otherwise stamps the *builder's* OS version as the minimum,
          # which would refuse to start on an older Mac for no reason. 11.0 is
          # where Apple Silicon began, so that is the floor worth publishing.
          - name: macos-arm64
            runs-on: macos-14
            asset: cpcpub-macos-arm64
            target: aarch64
            deployment-target: "11.0"
          # Windows, with the same -march pair as the two Linux x86 rows above
          # and for the same reasons: a portable baseline, and a v3 build where
          # the four variants differ. Keeping the flags identical is what makes
          # a Windows number and a Linux number from the same CPU a comparison
          # of two operating systems rather than of two compilations.
          #
          # The image's mingw-w64 gcc is the compiler: it is GCC 15 against the
          # UCRT, so these binaries are closer to their Linux counterparts than
          # a clang build would be. Nothing installs, which is also why these
          # rows are cheap.
          - name: windows-x86_64
            runs-on: windows-2025
            asset: cpcpub-windows-x86_64.exe
            exe: .exe
            vars: MARCH=x86-64 MTUNE=generic
            target: x86_64
          - name: windows-x86_64-v3
            runs-on: windows-2025
            asset: cpcpub-windows-x86_64-v3.exe
            exe: .exe
            vars: MARCH=x86-64-v3 MTUNE=generic
            target: x86_64
          # Windows on Arm. armv8-a to match the Linux aarch64 row, not the
          # ARMv8.1 that Windows itself requires of a machine: the artifact is
          # for comparing against that row, and every Arm device Windows runs on
          # is past both floors anyway.
          #
          # llvm-mingw because nothing else targets this: mingw-w64 gcc has no
          # aarch64 Windows target at all. Pinned to a version *and* a digest --
          # a release asset can be replaced in place, and a toolchain swapped
          # under a build that publishes verified binaries is exactly the change
          # that should stop the job rather than ship.
          - name: windows-arm64
            runs-on: windows-11-arm
            asset: cpcpub-windows-arm64.exe
            exe: .exe
            vars: MARCH=armv8-a MTUNE=generic
            target: aarch64
            llvm-mingw: "20260616"
            llvm-mingw-sha256: 312593669435bd0bfc1a43ac3fba23c8b27e0610bade88b2738e5a01702a99ba

    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ env.TAG }}
          persist-credentials: false

      - name: Install the toolchain
        env:
          CROSS: ${{ matrix.cross }}
          MIN_GCC: ${{ matrix.min-gcc }}
          LLVM_MINGW: ${{ matrix.llvm-mingw }}
          LLVM_MINGW_SHA256: ${{ matrix.llvm-mingw-sha256 }}
        run: |
          set -eux
          if [ "$RUNNER_OS" = "Windows" ]; then
            # Neither compiler answers to `cc` here, so CC is always named. So
            # is OBJDUMP: the import-table check below is the same check on both
            # rows, run through whichever tool that row's toolchain brought.
            #
            # `make` is the mingw-w64 one the runner image installs, which on
            # the Arm image is an x86_64 build under emulation -- fine for a
            # build driver, and not the compiler, which is the part that has to
            # be native.
            command -v make >/dev/null || {
              echo "no make on this image; the runner image is expected to " \
                   "put the mingw-w64 one on PATH" >&2; exit 1; }
            if [ -n "$LLVM_MINGW" ]; then
              url="https://github.com/mstorsjo/llvm-mingw/releases/download"
              zip="llvm-mingw-$LLVM_MINGW-ucrt-aarch64.zip"
              curl -fsSL -o "$zip" "$url/$LLVM_MINGW/$zip"
              echo "$LLVM_MINGW_SHA256  $zip" | sha256sum -c -
              7z x -y "$zip" -o"$RUNNER_TEMP" > /dev/null
              # Two spellings of one directory, because two things read it.
              # GITHUB_PATH is consumed by the runner and wants a native path;
              # the export is for this step only, where bash wants a POSIX one.
              echo "$RUNNER_TEMP\\llvm-mingw-$LLVM_MINGW-ucrt-aarch64\\bin" \
                >> "$GITHUB_PATH"
              PATH="$(cygpath -u "$RUNNER_TEMP")/llvm-mingw-$LLVM_MINGW-ucrt-aarch64/bin:$PATH"
              export PATH
              aarch64-w64-mingw32-clang --version
              echo "CC=aarch64-w64-mingw32-clang" >> "$GITHUB_ENV"
              echo "OBJDUMP=llvm-objdump" >> "$GITHUB_ENV"
            else
              gcc --version
              echo "CC=gcc" >> "$GITHUB_ENV"        # the image's mingw-w64 gcc
              echo "OBJDUMP=objdump" >> "$GITHUB_ENV"
            fi
            exit 0
          fi
          if [ -z "$CROSS" ]; then
            cc --version
            exit 0
          fi
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            "libc6-dev-${{ matrix.target }}-cross" qemu-user
          # Newest first: this image's *default* cross-gcc predates several
          # RVA23 extension names, and a build that quietly targets less than
          # the profile is the one failure mode worth spending a loop on.
          found=""
          for v in 16 15 14 13 ""; do
            if sudo apt-get install -y --no-install-recommends "gcc${v:+-$v}-$CROSS"; then
              found="$CROSS-gcc${v:+-$v}"
              break
            fi
          done
          [ -n "$found" ] || { echo "no $CROSS cross-compiler on this image" >&2; exit 1; }
          "$found" --version
          # A row that needs a profile the compiler cannot name says so here,
          # rather than a hundred lines later as an -march parse error.
          if [ -n "$MIN_GCC" ]; then
            have=$("$found" -dumpversion | cut -d. -f1)
            [ "$have" -ge "$MIN_GCC" ] || {
              echo "$found is gcc $have; this target needs $MIN_GCC or newer." \
                   "Move this matrix row to an image that ships one." >&2
              exit 1
            }
          fi
          echo "CC=$found" >> "$GITHUB_ENV"

      # CC comes from $GITHUB_ENV for a cross build and is unset for a native
      # one, where the Makefile's own default is right.
      - name: Build
        env:
          MACOSX_DEPLOYMENT_TARGET: ${{ matrix.deployment-target }}
        run: |
          set -eux
          make -C bench ${{ matrix.make-target || 'all' }} ${{ matrix.vars }}
          # No file(1) in Git Bash, and nothing here depends on its output.
          command -v file >/dev/null && file "$BIN" || ls -l "$BIN"

      # What the binary requires of the machine that runs it, in both senses:
      # the ISA it was compiled for, and the glibc it was linked against. Both
      # are published facts about a release artifact, so both are printed -- and
      # for the profile targets, asserted.
      - name: Check what it requires
        env:
          CROSS: ${{ matrix.cross }}
          NAME: ${{ matrix.name }}
        run: |
          set -eux
          # Mach-O has no glibc and no readelf. The equivalent published fact is
          # the minimum OS version stamped into LC_BUILD_VERSION, plus the fact
          # that this is a single-architecture arm64 file and not a fat binary.
          if [ "$RUNNER_OS" = "macOS" ]; then
            otool -L "$BIN"
            vtool -show-build "$BIN" || otool -l "$BIN" | grep -A4 LC_BUILD_VERSION
            archs=$(lipo -archs "$BIN")
            [ "$archs" = "arm64" ] || { echo "expected arm64, got $archs" >&2; exit 1; }
            # arm64 macOS refuses to execute an unsigned binary at all. The
            # linker signs ad hoc; if that ever stops happening, the smoke test
            # below would fail with a signal rather than a message, so check here.
            codesign -dv "$BIN"
            exit 0
          fi
          # PE has no glibc floor either. What it has is an import table, which
          # is the list of DLLs this binary will go looking for on a machine
          # that has nothing but Windows on it. Both assertions below are about
          # a binary that starts here and fails there; the architecture is not
          # checked here because the identity step already asserts the target
          # the binary compiled itself for.
          if [ "$RUNNER_OS" = "Windows" ]; then
            "$OBJDUMP" -p "$BIN" | sed -n 's/^[[:space:]]*DLL Name:[[:space:]]*//p' \
              | tr 'A-Z' 'a-z' | sort -u > dlls.txt
            cat dlls.txt
            # The UCRT, not msvcrt. A mingw-w64 built against msvcrt substitutes
            # a printf that predates C99, and %zu then prints as the letters
            # rather than the number -- in a document meant to be machine-read,
            # and without failing anything.
            grep -qE '^(api-ms-win-crt-|ucrtbase\.dll)' dlls.txt || {
              echo "not linked against the UCRT: $(cat dlls.txt)" >&2; exit 1; }
            ! grep -qx 'msvcrt\.dll' dlls.txt || {
              echo "linked against msvcrt, which has no C99 printf" >&2; exit 1; }
            # No toolchain DLLs. mingw's runtime ships as libwinpthread-1.dll
            # and friends unless -static says otherwise, and an artifact that
            # needs them beside it is not one anybody can just download.
            ! grep -q '^lib' dlls.txt || {
              echo "imports a toolchain DLL; the -static in bench/Makefile did " \
                   "not take effect" >&2; exit 1; }
            exit 0
          fi
          READELF="${CROSS:+$CROSS-}readelf"
          OBJDUMP="${CROSS:+$CROSS-}objdump"
          "$OBJDUMP" -T "$BIN" | grep -o 'GLIBC_[0-9.]*' | sort -uV | tail -3
          if [ "${CROSS:-}" ]; then "$READELF" -A "$BIN"; fi
          python3 - <<'PY'
          import glob, os, re, subprocess, sys

          name, cross = os.environ["NAME"], os.environ.get("CROSS", "")
          if not name.startswith("riscv64"):
              sys.exit(0)

          def isa_of(path):
              """The ISA an ELF file declares, as a set of bare extension names.

              Versions are stripped (`v1p0` -> `v`) and Zba/Zbb/Zbs may arrive
              folded into a single `b`, so both spellings are accepted below.
              """
              out = subprocess.run([f"{cross}-readelf", "-A", path],
                                   capture_output=True, text=True).stdout
              found = re.search(r'Tag_RISCV_arch: "([^"]+)"', out)
              if not found:
                  sys.exit(f"{path} declares no RISC-V arch attribute")
              return {re.sub(r"\d+p\d+$", "", tok)
                      for tok in found.group(1).split("_")}

          # Our own objects, not the linked binary. A link merges the attributes
          # of everything that went into it, including the distribution's crt
          # objects -- and on a host whose riscv64 baseline is already RVA23
          # those alone make any executable claim RVA23. The objects say what
          # the compiler emitted for *this* source, which is the question.
          ours = set()
          for obj in sorted(glob.glob("bench/src/*.o")):
              ours |= isa_of(obj)
          print("compiled for:", " ".join(sorted(ours)))

          if name == "riscv64-rva23":
              # The RVA23U64 extensions that reach codegen.
              want = {"v", "zicond", "zimop", "zcmop", "zcb", "zfa", "zawrs",
                      "zfhmin", "zvbb", "zvfhmin", "zvkt", "zkt"}
              missing = want - ours
              if not ({"zba", "zbb", "zbs"} <= ours or "b" in ours):
                  missing |= {"zba/zbb/zbs"}
              if missing:
                  sys.exit(f"not an RVA23 build -- missing {sorted(missing)}. "
                           "The cross-compiler is probably too old to know these "
                           "extension names; give this matrix row a newer image.")
              print("RVA23U64 confirmed")
              sys.exit(0)

          # The portable build must not have picked up a vector unit, or it
          # stops running on the very boards it exists for -- and here the
          # *linked* binary is the thing to check, because a runtime built for a
          # richer baseline drags the whole artifact up with it.
          if {"v"} & ours or any(x.startswith("zve") for x in ours):
              sys.exit(f"rv64gc build compiled vector instructions: {sorted(ours)}")
          whole = isa_of(os.environ["BIN"])
          if {"v"} & whole or any(x.startswith("zve") for x in whole):
              sys.exit(
                  "this image's riscv64 runtime is not rv64gc: the linked binary "
                  f"requires {sorted(x for x in whole if x == 'v' or x.startswith('zve'))} "
                  "even though our objects do not. Ubuntu moved its riscv64 "
                  "baseline to RVA23 in 26.04, so the portable artifact has to "
                  "be built on an older image than that -- which is why this job "
                  "pins one.")
          print("rv64gc confirmed scalar, runtime included")
          PY

      # A binary nobody has run is not a release artifact. Native targets run
      # directly; cross-built ones run under qemu with the CPU model the -march
      # claims, which is what catches a build emitting instructions the ISA
      # string does not promise.
      - name: Smoke test
        env:
          CROSS: ${{ matrix.cross }}
          QEMU_CPU: ${{ matrix.qemu-cpu }}
        run: |
          set -eux
          RUN=""
          if [ -n "$CROSS" ]; then
            RUN="qemu-${{ matrix.target }} -cpu $QEMU_CPU -L /usr/$CROSS"
          fi
          $RUN "./$BIN" --list-variants
          $RUN "./$BIN" --full --cpus 0 --threads 1 \
              --time 0.05 --reps 1 --warmup 0.02 --json > run.json

      # The verification chain in one step: what the binary says it is has to be
      # what the file is. If these ever disagree, every digest published below
      # names something that does not exist and nothing would verify.
      - name: Check the binary identifies itself
        env:
          NAME: ${{ matrix.name }}
          TARGET: ${{ matrix.target }}
          ASSET: ${{ matrix.asset }}
        run: |
          set -eux
          # macOS ships shasum, not sha256sum. The digest that counts is taken
          # in python below; this line is for the log.
          command -v sha256sum >/dev/null && sha256sum "$BIN" \
            || shasum -a 256 "$BIN"
          python3 - <<'PY'
          import hashlib, json, os, pathlib, re

          doc = json.load(open("run.json"))
          assert doc["schema"] == "cpu-bench/1", doc.get("schema")
          claimed = doc["build"]["binary_sha256"]
          actual = hashlib.sha256(pathlib.Path(os.environ["BIN"]).read_bytes()).hexdigest()
          assert re.fullmatch(r"[0-9a-f]{64}", claimed), claimed
          assert claimed == actual, f"binary says {claimed}, file is {actual}"

          # The binary has to agree with the matrix about what it is. Without
          # this the manifest would publish whatever the matrix row asserted,
          # so a target whose predefined macros the source does not recognise
          # would ship as "unknown" under a correct-looking label -- which is
          # exactly the failure a newly added architecture makes.
          reported = doc["build"]["target"]
          assert reported == os.environ["TARGET"], (
              f'binary reports target "{reported}", matrix says '
              f'"{os.environ["TARGET"]}" -- arch_string() in bench.c does not '
              "recognise this architecture's predefined macros")

          # What this build actually got, read out of its own output rather
          # than repeated from the matrix -- `make rva23` chooses its -march at
          # build time, so the matrix does not know it.
          flags = doc["build"]["flags"]
          march = re.search(r"-march=(\S+)", flags)
          info = {"filename": os.environ["ASSET"],
                  "target": os.environ["TARGET"],
                  "march": march.group(1) if march else None,
                  "flags": flags,
                  "compiler": doc["build"]["compiler_version"]}
          pathlib.Path("dist").mkdir(exist_ok=True)
          json.dump(info, open(f"dist/buildinfo-{os.environ['NAME']}.json", "w"),
                    indent=2)
          print(json.dumps(info, indent=2))
          PY
          cp "$BIN" "dist/$ASSET"

      - uses: actions/upload-artifact@v7
        with:
          name: build-${{ matrix.name }}
          path: dist/
          if-no-files-found: error
          retention-days: 7

  publish:
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write        # the only write in this workflow: attach assets
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: staged
          merge-multiple: true

      # Digests are taken here, over the exact bytes about to be uploaded --
      # not carried over from the build jobs. The file being published is the
      # only one whose digest is worth anything.
      - name: Checksums and manifest
        env:
          REPO: ${{ github.repository }}
          SERVER: ${{ github.server_url }}
        run: |
          set -eux
          cd staged
          # The upload/download round trip does not preserve the executable
          # bit, and an asset nobody can run is not a release.
          chmod +x cpcpub-*
          sha256sum cpcpub-* > SHA256SUMS
          cat SHA256SUMS
          python3 - <<'PY'
          import hashlib, json, os, pathlib

          # Driven by the buildinfo files rather than by the asset names: each
          # one names the asset its job produced, so a build that failed to
          # stage its binary is a missing file here instead of a release that
          # is quietly one architecture short.
          builds = []
          for meta in sorted(pathlib.Path(".").glob("buildinfo-*.json")):
              info = json.load(open(meta))
              path = pathlib.Path(info["filename"])
              assert path.is_file(), f"{meta} names {path}, which was not staged"
              builds.append({
                  "filename": path.name,
                  "sha256": hashlib.sha256(path.read_bytes()).hexdigest(),
                  "target": info["target"],
                  "march": info["march"],
              })
          assert builds, "nothing to publish"

          # Every staged binary must be accounted for, in the other direction:
          # a file with no buildinfo never went through the identity check.
          staged = {p.name for p in pathlib.Path(".").glob("cpcpub-*")}
          named = {b["filename"] for b in builds}
          assert staged == named, f"staged {sorted(staged)}, manifest {sorted(named)}"

          tag = os.environ["TAG"]
          manifest = {
              "schema": "cpu-bench-builds/1",
              "release": tag,
              "url": f"{os.environ['SERVER']}/{os.environ['REPO']}/releases/tag/{tag}",
              "builds": builds,
          }
          json.dump(manifest, open("verified-builds.json", "w"), indent=2)
          print(json.dumps(manifest, indent=2))
          PY
          # The manifest must agree with the checksum file, or the two halves of
          # the release contradict each other.
          python3 - <<'PY'
          import json
          published = {b["sha256"]: b["filename"]
                       for b in json.load(open("verified-builds.json"))["builds"]}
          listed = {}
          for line in open("SHA256SUMS"):
              digest, name = line.split()
              listed[digest] = name.lstrip("*")
          assert published == listed, (published, listed)
          print(f"{len(listed)} binaries agree between SHA256SUMS and the manifest")
          PY

      # `gh` reads the token from the environment, so it never reaches a process
      # list or a log line. --clobber so re-running the workflow for a tag
      # replaces the assets rather than failing on them.
      - name: Attach to the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eu
          cd staged
          gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1 \
            || gh release create "$TAG" --repo "$GITHUB_REPOSITORY" \
                 --title "$TAG" --generate-notes
          gh release upload "$TAG" --repo "$GITHUB_REPOSITORY" --clobber \
            cpcpub-* SHA256SUMS verified-builds.json

      - name: Say how to trust these
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
          ## Verified builds for \`$TAG\`

          Download a binary and check it against \`SHA256SUMS\`. A result
          produced by one of them shows as **verified** on a hub whose operator
          has run:

          \`\`\`sh
          python3 web/verified.py --db runs.sqlite3 \\
            $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases/download/$TAG/verified-builds.json
          \`\`\`

          The benchmark hashes its own binary and reports it as
          \`build.binary_sha256\`, so the hub matches on its own without being
          told anything by the submitter. It is not proof -- that field is
          self-reported like every other one -- but two results carrying the
          same digest were produced by the same machine code.
          EOF
